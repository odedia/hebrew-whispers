---
applications:
- name: whisper-app
  memory: 8G
  disk_quota: 20G
  instances: 1
  timeout: 600
  command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
  docker:
    image: <YOUR_REGISTRY>/whisper-app:v1
  env:
    MODELS_PATH: /app/models
